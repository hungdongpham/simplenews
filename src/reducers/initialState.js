import * as key from '../store/constants';

// initial state
const initialState = {
  [key.ERRORS]: {},
  [key.THEME]: true,
  [key.LOADING]: {},
  [key.NEWS]: [],
  [key.HEADLINE_NEWS]: [],
  [key.USER_INFO]: {}
};

export default initialState;
